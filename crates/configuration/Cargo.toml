[package]
name = "configuration"
version = "0.1.0"
edition = "2024"
description = "Configuration."

[dependencies]
logger = { path = "../logger" }
thiserror = "2.0.18"
serde_json = "1"
toml = "0.8"
config = "0.15.25"
serde = { version = "1.0.228", features = ["derive"] }
garde = { version = "0.23.0", features = ["full"] }
