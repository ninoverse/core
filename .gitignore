/target
.tmp
**/*.rs.bk

# generated analysis reports
/clippy-report.json
/lcov.info
/.scannerwork

# SonarQube token (never commit)
sonarqube/.sonar-token

# provisioned oauth2 client secrets, apisix session secret and admin key
secrets/

# editor
.vscode/
